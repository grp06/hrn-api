const makePairings = (onlineUsers, partnersRows) => {
  console.log('partnersRows', partnersRows)
  console.log('onlineUsers[0].tags_users', onlineUsers[0].tags_users)
  // run matching algorithm

  return [
    [1, 2],
    [3, 4],
  ]
}

export default makePairings
